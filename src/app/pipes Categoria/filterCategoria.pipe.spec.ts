import { FilterPipe1 } from './filterCategoria.pipe';

describe('FilterPipe1', () => {
  it('create an instance', () => {
    // const pipe = new FilterPipe1();
    // expect(pipe).toBeTruthy();
  });
});